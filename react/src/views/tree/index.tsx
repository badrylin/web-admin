import React from 'react';
import { Tree } from 'antd';
const { TreeNode } = Tree;

export default class TreePage extends React.Component {
  render() {
    return (
      <div>
        Tree
      </div>
    );
  }
}
